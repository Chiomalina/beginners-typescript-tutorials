// Declaring a tuple with two elements: a string and a number
let user: [string, number] = ["Alice", 30];

// Accessing tuple elements by index
const userName: string = user[0]; // "Alice"
const userAge: number = user[1];

// Tuple with three different types
let point: [number, number, string] = [10, 20, "center"];

// Changing tuple values
user[1] = 31;
